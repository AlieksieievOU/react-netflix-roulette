import React from 'react';
import styles from './MovieAdd.module.scss';

const MovieAdd = () => {
    return (
        <div className={styles.MovieAddWrapper}>
            + add movie
        </div>
    );
};

export default MovieAdd;